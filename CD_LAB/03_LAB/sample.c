#include <stdio.h>
#define MAX 100

// single line comment

/*
   multi
   line
   comment
*/

int main() {

    float a=10,b=20;
    double result = a+b;

    if (a <= b && b != 0 || a == 10) {
    	if (a >= b && a != b) {
        	return 1;
    	}
    }
    
    printf("A string literal");

    return 0;
}

